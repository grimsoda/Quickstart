# Quickstart Widgets

Widget implementation placeholder.

Plan for v1:

- Small widget: Top Do (2 min) + Top Tomorrow-win (10 min) + Open Menu.
- Medium widget: Do/Decide/Drift columns with 1–2 items each.

Implementation approach:

1. Use Expo `expo-widgets` (alpha) to declare widget families and render content
2. Share data via App Group defaults or shared storage snapshot
3. Use simulator build for automated testing
