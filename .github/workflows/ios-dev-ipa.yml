name: ios-dev-ipa

on:
  workflow_dispatch:

jobs:
  build-ios:
    runs-on: macos-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: "1.3.5"

      - name: Install dependencies
        run: bun install

      - name: Prebuild iOS project
        working-directory: apps/mobile
        run: bunx expo prebuild --platform ios --clean --no-install

      - name: Install CocoaPods
        run: sudo gem install cocoapods

      - name: Install iOS pods
        working-directory: apps/mobile
        run: pod install --project-directory=ios

      - name: Build unsigned app
        working-directory: apps/mobile
        run: |
          xcodebuild \
            -workspace ios/Quickstart.xcworkspace \
            -scheme Quickstart \
            -configuration Release \
            -sdk iphoneos \
            -derivedDataPath build/DerivedData \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGN_IDENTITY="" \
            clean build

      - name: Package unsigned IPA
        working-directory: apps/mobile
        run: |
          mkdir -p build/Payload
          cp -R build/DerivedData/Build/Products/Release-iphoneos/Quickstart.app build/Payload/
          cd build
          zip -r quickstart-unsigned.ipa Payload

      - name: Upload IPA
        uses: actions/upload-artifact@v4
        with:
          name: quickstart-unsigned-ipa
          path: apps/mobile/build/quickstart-unsigned.ipa
